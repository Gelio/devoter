/**
 * Used in the Poll class
 *
 * @class Option
 * @param {String} name
 * @param {Number} amount
 * @constructor
 */
var Option = function(name, amount) {
    this.name = name;
    this.amount = amount;
};